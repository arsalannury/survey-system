import {RegisterIcons} from './RegisterStyle';

export const BadgeIcon: JSX.Element = (
    <span style={RegisterIcons} className="material-symbols-outlined">
    badge
  </span>
);

export const BadgeIconEmail: JSX.Element = (
    <span style={RegisterIcons} className="material-symbols-outlined">
    mail
  </span>
);

export const BadgeIconPassword: JSX.Element = (
    <span style={RegisterIcons} className="material-symbols-outlined">
    lock
  </span>
);

export const BadgeIconShowPassword: JSX.Element = (
    <span style={RegisterIcons} className="material-symbols-outlined">
    no_encryption
  </span>
);

export const BadgeIconUerName: JSX.Element = (
    <span style={RegisterIcons} className="material-symbols-outlined">
     person
    </span>
);
